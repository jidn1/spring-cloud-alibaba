server:
  port: 8081
spring:
  profiles:
    active: dev
  application:
    name: spring-cloud-alibaba-nacos
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yaml

